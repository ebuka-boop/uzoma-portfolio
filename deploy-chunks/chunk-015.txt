le('[data-cycle="a"]',2200,0);cycle('[data-cycle="b"]',2600,900);
const loader=document.querySelector('.loader'),bar=document.querySelector('.loader-bar i'),count=document.querySelector('.load-count'),words=[...document.querySelectorAll('.loader-center span')];words.forEach((w,i)=>setTimeout(()=>w.style.transition='transform .8s cubic-bezier(.2,.8,.2,1)',20+i*40));requestAnimationFrame(()=>words.forEach(w=>w.style.transform='translateY(0)'));
let p=0;const li=setInterval(()=>{p=Math.min(100,p+Math.ceil(Math.random()*8));count.textContent=String(p).padStart(2,'0');bar.style.width=p+'%';if(p>=100){clearInterval(li);setTimeout(()=>{loader.style.transition='transform .8s cubic-bezier(.77,0,.18,1)';loader.style.transform='translateY(-101%)'},250)}},55);
const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('in')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));
const mh=document.querySelector('.manifesto h2');mh.innerHTML=mh.textContent.trim().split(/\s+/).map(w=>`<span class="w">${w}</span>`).join(' ');const wordEls=[...mh.querySelectorAll('.w')];function paintWords(){const r=mh.getBoundingClientRect(),vh=innerHeight,prog=Math.max(0,Math.min(1,(vh-r.top)/(vh+r.height*.55)));const lit=Math.floor(prog*wordEls.length*1.15);wordEls.forEach((w,i)=>w.style.opacity=i<lit?1:.18)}addEventListener('scroll',paintWords,{passive:true});paintWords();
const cursor=document.querySelector('.cursor');if(matchMedia('(pointer:fine)').matches){addEventListener('mousemove',e=>{cursor.style.opacity=1;cursor.style.transform=`translate(${e.clientX}px,${e.clientY}px)`});document.querySelectorAll('a,.email-tile').forEach(el=>{el.addEventListener('mouseenter',()=>{cursor.classList.add('on');cursor.textContent=el.dataset.cursor||'OPEN'});el.addEventListener('mouseleave',()=>{cursor.classList.remove('on');cursor.textContent='VIEW'})})}
const nums=document.querySelectorAll('.num');const nio=new IntersectionObserver(entries=>entries.forEach(e=>{if(!e.isIntersecting||e.target.dataset.done)return;e.target.dataset.done=1;const el=e.target,target=parseFloat(el.dataset.target),pre=el.dataset.prefix||'',suf=el.dataset.suffix||'';let start=performance.now();function tick(t){const x=Math.min(1,(t-start)/1100),v=target*x;el.textContent=pre+(target%1? v.toFixed(1):Math.round(v))+suf;if(x<1)requestAnimationFrame(tick)}requestAnimationFrame(tick)}),{threshold:.5});nums.forEach(n=>nio.observe(n));
</script>
</body></html>
